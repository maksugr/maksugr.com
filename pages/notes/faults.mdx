import { NoteLayout } from '../../components/note-layout';

export const meta = {
    title: 'Faults',
    slug: 'faults'
};

## Faults

export default ({ children }) => <NoteLayout meta={meta}>{children}</NoteLayout>
